spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    #    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/studs}
    #    username: ${SPRING_DATASOURCE_USERNAME:s367044}
    #    password: ${SPRING_DATASOURCE_PASSWORD:rkemWfU26OYiwbkD}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:mihailagei}
    password: ${SPRING_DATASOURCE_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:01ebc0bb5e1c88a20643ecf10712e286a367be48756f031df0b51e70911ed542}

logging:
  level:
    org:
      hibernate:
        type: info

server:
  port: 18123

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
